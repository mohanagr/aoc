with open('temp.txt') as f:
    x=f.read().strip().split('\n\n')

def cmpr(exp1,exp2):
    a=exp1.pop(0)
    b=exp2.pop(0)
    if()
for l in x:
    l1,l2=[a[1:-1] for a in l.split('\n')]
    print(l1,l2,len(l1), len(l2))